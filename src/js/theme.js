// If jQuery is needed
// jQuery(function () {});

// Once the document is ‘ready’ to be manipulated
document.addEventListener("DOMContentLoaded", function () {}, false);

// there could still be resources that are being loaded (e.g. images, external stylesheets, etc)
// use this when manipulating elements once the full page has fully loaded
// window.addEventListener("load", function () {}, false);
